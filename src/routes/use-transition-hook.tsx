import UseTransitionHookPage from '@/app/use-transition-hook'
import { createFileRoute } from '@tanstack/react-router'

export const Route = createFileRoute('/use-transition-hook')({
  component: UseTransitionHookPage,
})
